<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

<comment>board-sql</comment>

	<entry key="selectBoard">
SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, CREATE_DT, MEMBER_NO
FROM BOARD 	
	</entry>

	<entry key="selectMyBoard">
SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, TO_CHAR(CREATE_DT, 'YYYY-MM-DD HH24:MI:SS') CREATE_DT, MEMBER_NO
FROM BOARD
LEFT JOIN MEMBER USING (MEMBER_NO)
WHERE MEMBER_ID = ?
	</entry>

	<entry key="insertBoard">
INSERT INTO BOARD 
VALUES (SEQ_COMMENT_NO.NEXTVAL, ?, ?, DEFAULT, DEFAULT, DEFAULT, ?)
	</entry>

	<entry key="selectMyBoard2">
SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, TO_CHAR(CREATE_DT, 'YYYY-MM-DD HH24:MI:SS') CREATE_DT, MEMBER_NO
FROM BOARD
LEFT JOIN MEMBER USING (MEMBER_NO)
WHERE MEMBER_NO = ?
	</entry>

	<entry key="updateBoard">
UPDATE BOARD
SET BOARD_TITLE = ?,
BOARD_CONTENT = ?
WHERE BOARD_NO = ?
	</entry>

	<entry key="deleteBoard">
DELETE FROM BOARD
WHERE BOARD_NO = ?
	</entry>

</properties>
